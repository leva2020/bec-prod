<nav class="sub panel panel-default">
    {% if terms %}
        <p class="heading primary_colour_1-text"><span>Categorías</span></p>
        <ul class="nav">
            {% for term in terms %}
                <li class="odd">
                    <a href="/publicaciones/?categoria={{ term }}" rel="NOINDEX, FOLLOW">
                        {{ term }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
</nav>