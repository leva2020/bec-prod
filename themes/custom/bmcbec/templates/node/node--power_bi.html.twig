{% include 'banners.html.twig' with {'data': node.field_image.entity } %}
<section class="content advanced-component grid secondary_colour_3-bg">
    <div class="" data-os-animation="fadeIn" data-os-animation-delay="0">
        <div class="umb-grid">
            <div class="grid-section">
                <div>
                    <div class="container">
                        <div class="row clearfix">
                            <div class="col-md-12 column">
                                <div>
                                    <h2 style="text-align: center;">{{ node.title.value }}</h2>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% for bi in node.field_tables %}
    <iframe id="BI3" width="100%" height="900"
            src="{{ bi.url }}"
            style="height:900px; border:0;" scrolling="no">
    </iframe>
{% endfor %}
<div class="item lists panel panel-default even">
    {{ drupal_block('recent_publications_block') }}
</div>