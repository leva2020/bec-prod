<section class="content advanced-component grid">
    <div class="" data-os-animation="fadeIn" data-os-animation-delay="0">
      <div class="container">
        <div class="row">
          {% for item in data %}
            <div class="col-md-6">
              <h2>{{item.entity.field_titulo.value }}</h2>
              <div class="wrapper-image">
                <img src="{{file_url(item.entity.field_imagen.entity.fileuri)}}" title="{{item.entity.field_imagen.title}}" alt="{{item.entity.field_imagenalt}}">
              </div>
              <div class="content">
                <blockquote>
                  {{item.entity.field_descripcion_1.value|raw }}
                </blockquote>
                <h3><a href="{{item.entity.field_link.url}}" title="{{item.entity.field_link.title}}">{{item.entity.field_link.title}}</a></h3>
              </div>
              <section class="content advanced-component">
                <div class="container">
                  <div class="row">
                    <div class="col-md-6 pull-right">
                      {% include 'page-component-accordion-1.html.twig' with {'data': item.entity.field_acordeon_1 } %}
                    </div>
                  </div>
                </div>
              </section>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>
